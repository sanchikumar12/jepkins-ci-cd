# Antigravity Service Testing Guide (Bypass Mode)

This document outlines the testing procedures for the **Payment Service** in **Bypass Mode** (All Security Disabled).

## 🚀 Service Information
- **Port:** 8086
- **Context Path:** `/api/v1`
- **Base URL:** `http://localhost:8086/api/v1`
- **Swagger UI:** `http://localhost:8086/api/v1/swagger-ui/index.html`
- **Test Endpoint:** `http://localhost:8086/api/v1/payments/test`

## 🛠 Security State: BYPASSED
I have completely disabled authentication in `SecurityConfig.java`:
- All `/api/v1/payments/**` endpoints are set to `permitAll()`.
- All `/api/v1/subscriptions/**` endpoints are set to `permitAll()`.
- **Form Login** and **HTTP Basic** have been explicitly disabled.

> **Note:** If you are still seeing a login page, please **restart the service in STS**. Spring Security changes require a full application context refresh.

## ✅ Verification Checklist
- [ ] Open [Swagger UI](http://localhost:8086/api/v1/swagger-ui/index.html) - Should load without login.
- [ ] Hit [/api/v1/payments/test](http://localhost:8086/api/v1/payments/test) - Should return a response.

---
*Generated by Antigravity AI*
